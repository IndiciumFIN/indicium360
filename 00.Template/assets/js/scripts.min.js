document.addEventListener('DOMContentLoaded',function(){const langTrigger = document.getElementById('langTrigger');const langMegaMenu = document.getElementById('langMegaMenu');const langSelector = document.querySelector('.lang-selector');if(langTrigger && langMegaMenu){const arrow = langTrigger.querySelector('.fa-chevron-down');langSelector.addEventListener('mouseenter',()=>{langMegaMenu.classList.add('active');if(arrow)arrow.style.transform = 'rotate(180deg)';});langSelector.addEventListener('mouseleave',()=>{langMegaMenu.classList.remove('active');if(arrow)arrow.style.transform = 'rotate(0deg)';});langMegaMenu.addEventListener('mouseenter',()=>{langMegaMenu.classList.add('active');if(arrow)arrow.style.transform = 'rotate(180deg)';});langMegaMenu.addEventListener('mouseleave',()=>{langMegaMenu.classList.remove('active');if(arrow)arrow.style.transform = 'rotate(0deg)';});langTrigger.addEventListener('click',(e)=>{e.stopPropagation();langMegaMenu.classList.toggle('active');if(langMegaMenu.classList.contains('active')){if(arrow)arrow.style.transform = 'rotate(180deg)';}else{if(arrow)arrow.style.transform = 'rotate(0deg)';}});document.addEventListener('click',(e)=>{if(!langSelector.contains(e.target)&& !langMegaMenu.contains(e.target)){langMegaMenu.classList.remove('active');if(arrow)arrow.style.transform = 'rotate(0deg)';}});}const btnLight = document.getElementById('btnLight');const btnDark = document.getElementById('btnDark');if(btnLight && btnDark){btnLight.addEventListener('click',()=>{document.documentElement.removeAttribute('data-theme');btnLight.classList.add('active');btnDark.classList.remove('active');localStorage.setItem('theme','light');});btnDark.addEventListener('click',()=>{document.documentElement.setAttribute('data-theme','dark');btnDark.classList.add('active');btnLight.classList.remove('active');localStorage.setItem('theme','dark');});const savedTheme = localStorage.getItem('theme');if(savedTheme === 'dark'){document.documentElement.setAttribute('data-theme','dark');btnDark.classList.add('active');btnLight.classList.remove('active');}}const btnFontIncrease = document.getElementById('btnFontIncrease');const btnFontDecrease = document.getElementById('btnFontDecrease');let currentFontSize = 16;const minFontSize = 12;const maxFontSize = 24;const savedFontSize = localStorage.getItem('fontSize');if(savedFontSize){currentFontSize = parseInt(savedFontSize);document.documentElement.style.setProperty('--font-size-base',currentFontSize + 'px');}if(btnFontIncrease){btnFontIncrease.addEventListener('click',()=>{if(currentFontSize < maxFontSize){currentFontSize += 2;document.documentElement.style.setProperty('--font-size-base',currentFontSize + 'px');localStorage.setItem('fontSize',currentFontSize);}});}if(btnFontDecrease){btnFontDecrease.addEventListener('click',()=>{if(currentFontSize > minFontSize){currentFontSize -= 2;document.documentElement.style.setProperty('--font-size-base',currentFontSize + 'px');localStorage.setItem('fontSize',currentFontSize);}});}const navItems = document.querySelectorAll('.nav-item');navItems.forEach(item =>{const megaMenu = item.querySelector('.mega-menu-wrapper');if(megaMenu){item.addEventListener('mouseenter',()=>{megaMenu.classList.add('active');});item.addEventListener('mouseleave',()=>{megaMenu.classList.remove('active');});}});const skipLinks = document.querySelectorAll('.skip-links a');skipLinks.forEach(link =>{link.addEventListener('click',(e)=>{e.preventDefault();const targetId = link.getAttribute('href');const targetElement = document.querySelector(targetId);if(targetElement){targetElement.scrollIntoView({behavior: 'smooth',block: 'start'});targetElement.focus();}});});if('loading' in HTMLImageElement.prototype){const images = document.querySelectorAll('img[loading="lazy"]');images.forEach(img =>{img.src = img.dataset.src;});}else{const script = document.createElement('script');script.src = 'https: document.body.appendChild(script);}const manageCookiesBtn = document.getElementById('manage-cookies-footer-btn');if(manageCookiesBtn){manageCookiesBtn.addEventListener('click',()=>{alert('Gerenciador de Cookies - Em desenvolvimento');});}const backToTopBtn = document.getElementById('backToTopBtn');if(backToTopBtn){backToTopBtn.addEventListener('click',()=>{window.scrollTo({top: 0,behavior: 'smooth'});});}const searchInput = document.querySelector('.search-input');const searchBtn = document.querySelector('.search-btn');const searchWrap = document.querySelector('.search-wrap');function performSearch(){if(searchInput && searchInput.value.trim()!== ''){const searchTerm = searchInput.value.trim();window.location.href = `https:}}if(searchBtn){searchBtn.addEventListener('click',performSearch);}if(searchInput){searchInput.addEventListener('keypress',(e)=>{if(e.key === 'Enter'){performSearch();}});}document.addEventListener('keydown',(e)=>{if(e.key === 'Tab'){document.body.classList.add('keyboard-navigation');}});document.addEventListener('mousedown',()=>{document.body.classList.remove('keyboard-navigation');});if(window.performance && window.performance.timing){window.addEventListener('load',()=>{const timing = window.performance.timing;const pageLoadTime = timing.loadEventEnd - timing.navigationStart;console.log('Tempo de carregamento da página:',pageLoadTime,'ms');});}window.addEventListener('error',(e)=>{console.error('Erro capturado:',e.message);});if('serviceWorker' in navigator){}});function debounce(func,wait){let timeout;return function executedFunction(...args){const later =()=>{clearTimeout(timeout);func(...args);};clearTimeout(timeout);timeout = setTimeout(later,wait);};}function isElementInViewport(el){const rect = el.getBoundingClientRect();return(rect.top >= 0 && rect.left >= 0 && rect.bottom <=(window.innerHeight || document.documentElement.clientHeight)&& rect.right <=(window.innerWidth || document.documentElement.clientWidth));}function animateCounter(element,start,end,duration){let startTimestamp = null;const step =(timestamp)=>{if(!startTimestamp)startTimestamp = timestamp;const progress = Math.min((timestamp - startTimestamp)/ duration,1);element.textContent = Math.floor(progress *(end - start)+ start);if(progress < 1){window.requestAnimationFrame(step);}};window.requestAnimationFrame(step);}function formatDatePTBR(date){return new Intl.DateTimeFormat('pt-BR',{day: '2-digit',month: '2-digit',year: 'numeric'}).format(date);}async function copyToClipboard(text){try{await navigator.clipboard.writeText(text);return true;}catch(err){console.error('Erro ao copiar:',err);return false;}}if(typeof module !== 'undefined' && module.exports){module.exports ={debounce,isElementInViewport,animateCounter,formatDatePTBR,copyToClipboard};}window.dataLayer = window.dataLayer || [];function gtag(){dataLayer.push(arguments);}gtag('consent','default',{'analytics_storage': 'denied','ad_storage': 'denied','ad_user_data': 'denied','ad_personalization': 'denied','wait_for_update': 500});const ConsentManager ={STORAGE_KEY: 'cookie_consent',init(){const consent = this.getConsent();if(consent === null){this.showBanner();}else if(consent === 'accepted'){this.updateGtagConsent(true);}this.bindEvents();},getConsent(){return localStorage.getItem(this.STORAGE_KEY);},setConsent(value){localStorage.setItem(this.STORAGE_KEY,value);},showBanner(){const banner = document.querySelector('.consent-banner');if(banner){setTimeout(()=>{banner.classList.add('active');},1000);}},hideBanner(){const banner = document.querySelector('.consent-banner');if(banner){banner.classList.remove('active');}},acceptAll(){this.setConsent('accepted');this.updateGtagConsent(true);this.hideBanner();},rejectAll(){this.setConsent('rejected');this.updateGtagConsent(false);this.hideBanner();},updateGtagConsent(granted){if(typeof gtag === 'function'){gtag('consent','update',{'analytics_storage': granted ? 'granted' : 'denied','ad_storage': granted ? 'granted' : 'denied','ad_user_data': granted ? 'granted' : 'denied','ad_personalization': granted ? 'granted' : 'denied'});}},bindEvents(){document.addEventListener('click',(e)=>{if(e.target.matches('.consent-btn-accept')){this.acceptAll();}else if(e.target.matches('.consent-btn-reject')){this.rejectAll();}});}};document.addEventListener('DOMContentLoaded',()=>{ConsentManager.init();});function loadVLibras(){if(document.querySelector('[vw]')){const script = document.createElement('script');script.src = 'https: script.onload = function(){if(window.VLibras){new window.VLibras.Widget('https:}};document.body.appendChild(script);}}window.addEventListener('load',loadVLibras);function generateBreadcrumb(items){const container = document.querySelector('.breadcrumb-list');if(!container || !items || !items.length)return;container.innerHTML = items.map((item,index)=>{const isLast = index === items.length - 1;if(isLast){return `<li class="breadcrumb-item"> <span class="breadcrumb-current">${item.label}</span> </li>`;}return `<li class="breadcrumb-item"> <a href="${item.url}">${item.label}</a> <span class="breadcrumb-separator">/</span> </li>`;}).join('');}async function loadFooter(footerPath){const footerPlaceholder = document.getElementById('footer-placeholder');if(!footerPlaceholder)return;try{const response = await fetch(footerPath || '../0.Template/footer.html');if(response.ok){const footerHTML = await response.text();footerPlaceholder.innerHTML = footerHTML;}else{console.warn('Footer não encontrado');}}catch(error){console.error('Erro ao carregar footer:',error);}}function printPage(){window.print();}async function sharePage(title,text){const shareData ={title: title || document.title,text: text || document.querySelector('meta[name="description"]')?.content || '',url: window.location.href};try{if(navigator.share){await navigator.share(shareData);}else{await copyToClipboard(window.location.href);alert('Link copiado para a área de transferência!');}}catch(err){console.error('Erro ao compartilhar:',err);}}function calculateReadingTime(text,wordsPerMinute = 200){const words = text.trim().split(/\s+/).length;const minutes = Math.ceil(words / wordsPerMinute);return minutes;}function displayReadingTime(selector){const content = document.querySelector(selector);const display = document.querySelector('.reading-time');if(content && display){const minutes = calculateReadingTime(content.textContent);display.textContent = `${minutes}min de leitura`;}}function scrollToElement(selector,offset = 100){const element = document.querySelector(selector);if(element){const top = element.getBoundingClientRect().top + window.pageYOffset - offset;window.scrollTo({top: top,behavior: 'smooth'});}}function generateTableOfContents(contentSelector,tocSelector){const content = document.querySelector(contentSelector);const toc = document.querySelector(tocSelector);if(!content || !toc)return;const headings = content.querySelectorAll('h2,h3');const tocList = document.createElement('ul');tocList.className = 'toc-list';headings.forEach((heading,index)=>{if(!heading.id){heading.id = `section-${index}`;}const li = document.createElement('li');li.className = `toc-item toc-${heading.tagName.toLowerCase()}`;const link = document.createElement('a');link.href = `#${heading.id}`;link.textContent = heading.textContent;link.addEventListener('click',(e)=>{e.preventDefault();scrollToElement(`#${heading.id}`);});li.appendChild(link);tocList.appendChild(li);});toc.appendChild(tocList);}function initializeExpandableMenus(){const expandableTitles = document.querySelectorAll('.expandable-title');expandableTitles.forEach(title =>{title.addEventListener('click',function(e){e.stopPropagation();this.classList.toggle('expanded');const content = this.nextElementSibling;if(content && content.classList.contains('mm-expandable-content')){content.classList.toggle('expanded');}});});}if(document.readyState === 'loading'){document.addEventListener('DOMContentLoaded',initializeExpandableMenus);}else{initializeExpandableMenus();}function initializeSidebarNavigation(){const navItems = document.querySelectorAll('.mm-nav-item');navItems.forEach(item =>{item.addEventListener('click',function(e){e.preventDefault();e.stopPropagation();const targetId = this.getAttribute('data-target');if(!targetId)return;const container = this.closest('.mm-sidebar-layout');if(!container)return;container.querySelectorAll('.mm-nav-item').forEach(navItem =>{navItem.classList.remove('active');});this.classList.add('active');container.querySelectorAll('.mm-section-content').forEach(section =>{section.classList.remove('active');});const targetSection = container.querySelector(`#${targetId}`);if(targetSection){targetSection.classList.add('active');}});});document.querySelectorAll('.mm-sidebar-layout').forEach(sidebar =>{const firstNavItem = sidebar.querySelector('.mm-nav-item');const firstSection = sidebar.querySelector('.mm-section-content');if(firstNavItem && firstSection){firstNavItem.classList.add('active');firstSection.classList.add('active');}});}if(document.readyState === 'loading'){document.addEventListener('DOMContentLoaded',initializeSidebarNavigation);}else{initializeSidebarNavigation();}